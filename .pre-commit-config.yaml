repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks (secret scan)
        description: Detect hardcoded secrets in staged changes before commit
        entry: gitleaks protect --staged --redact -v
        language: system
        pass_filenames: false
